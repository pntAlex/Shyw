import "package:flutter/material.dart";

void main(BuildContext context) => runApp(MaterialApp(
      home: Scaffold(
      appBar: Appbar(title = "JDC"),
      body: Center(child : Text("TODO"))
      ),
      debugShowCheckedModeBanner: false,
    ));